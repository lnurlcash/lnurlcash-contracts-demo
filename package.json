{
  "name": "lnurlcash-contracts-demo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build",
    "acceptance:browser": "npm run acceptance:browser:ceremony && npm run acceptance:browser:hostile-mint && npm run acceptance:browser:mock-lifecycle",
    "acceptance:browser:ceremony": "node scripts/browser-acceptance.mjs",
    "acceptance:browser:hostile-mint": "node scripts/browser-hostile-mint.mjs",
    "acceptance:browser:mock-lifecycle": "node scripts/browser-mock-lifecycle.mjs"
  },
  "dependencies": {
    "@noble/hashes": "^2.3.0",
    "lnurlcash-kit": "^0.2.1",
    "nostr-tools": "2.24.1"
  },
  "devDependencies": {
    "happy-dom": "^20.11.2",
    "playwright": "1.62.1",
    "typescript": "^5.7.0",
    "vite": "^7.3.6",
    "vitest": "^3.0.0"
  }
}
